# Prompt templates (copy/paste)

## LiquidGlass component generation
**Prompt:**
> Create a SwiftUI component `GlassMetricCard` for iOS 26+. It must use LiquidGlass for the background, support Reduce Transparency, and have a watchOS variant optimized for small screens. Return file(s) with target membership notes, plus a snapshot test plan.

## HealthKit setup + permissions
**Prompt:**
> Implement HealthKit auth for reading step count and heart rate, and writing workouts. Provide entitlements and Info.plist strings. Use async/await. Include a unit-testable data transformation layer and an App Review checklist.

## HealthKit real-time updates
**Prompt:**
> Implement observer + anchored query pattern for heart rate updates. Ensure observer completion handler is called promptly. Provide a lightweight persistence strategy for anchors.

## WatchConnectivity request/response
**Prompt:**
> Implement WCSession managers on iOS and watchOS for request/reply of “today steps”. Include not-reachable fallback via applicationContext. Add a versioned message schema.

## Core ML pipeline (model-provided)
**Prompt:**
> Given this autogenerated model interface (paste it), build an inference manager with caching, preprocessing, and error handling. Provide a unit test stub that validates preprocessing and output mapping.

## End-to-end vertical slice
**Prompt:**
> Build a minimal vertical slice: iPhone shows a glass dashboard with today steps + current heart rate; watch shows a glanceable view; watch can request steps from phone. Provide file map, build commands, tests, and required permissions.
