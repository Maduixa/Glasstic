# UI tests

## iOS
- Permission-gated entry: ensure app does not crash when denied
- Dashboard loads and shows placeholders when no data

## watchOS
- Glance view loads without paired phone (should show ‘not connected’ state)
- Request steps when phone reachable and update UI
