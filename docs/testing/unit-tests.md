# Unit tests

## HealthKit
- Test date predicates (start-of-day boundaries)
- Test unit conversions (count, bpm, meters)
- Test mapping raw samples to UI models

## WatchConnectivity
- Test Codable message schema encoding/decoding
- Test versioning fallback behavior

## Core ML
- Test preprocessing (normalization, shapes) as pure functions
- If model included, test one “golden” input-output expectation
